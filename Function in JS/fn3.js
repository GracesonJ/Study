// w.a.p to find the sum of three numbers using arrow function.

const add = (n1,n2,n3)=>n1+n2+n3
console.log(add(20,40,40));

// w.a.p to find the square of a given number.

const sq = (num)=>num**2
console.log(sq(20));
